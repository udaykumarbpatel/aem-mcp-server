{
  "name": "aem-mcp-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace containing the legacy MCP server and the LangGraph-based authoring agent.",
  "scripts": {
    "build": "pnpm --filter aem-langgraph-agent build",
    "lint": "pnpm --filter aem-langgraph-agent lint",
    "test": "pnpm --filter aem-langgraph-agent test",
    "agent": "pnpm --filter aem-langgraph-agent agent"
  },
  "packageManager": "pnpm@8.15.4"
}
